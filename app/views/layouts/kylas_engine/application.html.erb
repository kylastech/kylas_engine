<!DOCTYPE html>
<html>
<head>
  <title>Kylas engine</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <meta name="robots" content="noindex">
  <meta property="og:image" content="https://assets.kylas.io/images/blue-logo.svg" />
  <%= stylesheet_link_tag "kylas_engine/application", media: "all" %>
  <%= javascript_include_tag "kylas_engine/application" %>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Rubik:wght@400&display=swap" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css" rel="stylesheet">
</head>
<body>
  <% if user_signed_in? %>
    <div class="d-flex flex-column h-100" id="app">
      <div class="d-flex flex-column h-100">
        <%= render 'shared/kylas_engine/navbar' %>
      </div>
    </div>
  <% else %>
    <%= yield %>
  <% end %>
</body>
</html>
